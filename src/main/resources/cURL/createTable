curl --location --request POST 'http://localhost:8080/createTable' \
--header 'Content-Type: application/json' \
--data-raw '{
    "element_name": "movie",
    "configuration": {
        "name": "VARCHAR(255)",
        "age": "INT",
        "nationality": "VARCHAR(255)",
        "address": "VARCHAR(255)"
    }
}'